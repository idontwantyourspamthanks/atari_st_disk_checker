# Dependencies
node_modules/

# Production build
dist/

# Distribution archives (npm run zip)
diskcheck-dist-*.zip

# Tooling caches
.vite/
*.tsbuildinfo

# Test output
coverage/

# Local env (not used today, but don't ship secrets by accident)
.env
.env.*
!.env.example

# OS junk
.DS_Store
Thumbs.db

# Editor / swap files
.idea/
.vscode/
*.swp
*~

# Logs
npm-debug.log*
*.log

# Optional local scanner corpus — not checked in; corpus test skips when absent
diskimages/
